package engine.factory

class PieceFactory {
    companion object {

        private var id = '0'
/*
        fun createPawn (color: Color) : Piece {
            val rules = listOf(
                StraightValidator(1),
                ColorValidator()
            )

            return Pawn("" + id++, color, rules )
        }

 */
    }
}